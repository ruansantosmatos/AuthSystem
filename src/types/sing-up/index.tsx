export enum fieldSingUp {
    firstName = "firstName",
    lastName = "lastName",
    email = "email",
    password = "password"
}

export interface ISingUp{
    firstName: string,
    lastName: string,
    email: string,
    password: string
}

